name: Build EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:

  build:

    runs-on: windows-latest

    steps:

    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Build exe
      run: |
        pyinstaller --noconfirm --onefile --windowed --icon=B16_flag_icon.ico main.py

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: SmartClassroom
        path: dist/main.exe
